[package]
name = "troute"
version = "0.1.0"
edition = "2021"
rust-version = "1.78"
description = "Routing and route optimization engine for Trasolve"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "1"

[dev-dependencies]
serde_json = "1"
